!!!
%html
  %head
    <link rel="shortcut icon" href='fav.ico'>
    :javascript
      window.api_url = "#{ENV['API_BASE_URL']}";
      window.access_token = "#{@user.token if @user && @user.token}";
    %title
      = @title || ENV['DEFAULT_TITLE']

  %body
    
    = yield
